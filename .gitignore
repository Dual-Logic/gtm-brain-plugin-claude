# Build artifacts. The installable .plugin bundle under dist/ is a release asset,
# not source; the tracked release zip is force-added past this rule.
dist/

# The working document is created in a user's own project/workspace at runtime,
# never here.
gtm-brain-spec.md

# Internal project-orientation for Claude Code sessions — kept local, not shipped.
/CLAUDE.md

# Internal planning/reference material (plans, conventions, validation tracking) —
# kept local, not shipped.
docs/

# OS / editor cruft
.DS_Store
